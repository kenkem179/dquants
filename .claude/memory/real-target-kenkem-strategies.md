---
name: real-target-kenkem-strategies
description: "The actual goal — optimize the user's existing MQL5 KenKem strategies, not invent new ones"
metadata: 
  node_type: memory
  type: project
  originSessionId: 93eabcdd-ed73-4c3a-8a0a-c1d68af64676
---

**RULE (source of truth):** the **MQL5 edition is the canonical reference for ALL strategies** — the
user has MQL5 for every one. **Do NOT read/cite Pine by default** (even though `kenkem-pine*` exists);
only consult a Pine script when the user *explicitly* points to a specific one. MQL5 is what ships and
what we port; Pine is incidental history. (The MQL5 is admittedly imperfect — port it faithfully
anyway, then improve via the data.)

**The real objective (clarified):** the dquants pipeline is infrastructure; the *actual* target is to
make the user's **existing, proven MQL5 strategies** data-driven and release-ready — NOT to discover
new strategies. So `/quant-hypothesis` is reframed: formalize the user's strategy as the thing to
optimize.

**Strategy repo:** `/Users/tokyotechies/Workspace/KEM/kenkem` (sibling of dquants). Strategies are
**Volume-Profile-centric** (matches dquants Phase-5 discovery: VP dominated SHAP). Families:
- `KK-MasterVP` (base, `MQL5/Experts/KK-MasterVP/`) — modular, the **first port target**. Known perf:
  PF 1.21 TRAIN / 1.10 OOS XAUUSD M3 (~1331 trades), below 1.25/1.15 gate. Edge is **tail-carried**
  (~20 trades ≈ 112% of profit); param sweeps came back null → needs structural improvement.
- `KK-MasterVP-Monster` — active evolution (adds multi-TF NetVolume + impulse-thrust). Phase-2 target.
- `KenKemExpert` (EMA E1–E5) — separate family.

**CRITICAL finding:** the MQL5 base EA is **NOT a 1:1 port of the Pine** the notes describe. Notes =
Pine (fresh-cross + multi-TF net 80% gates). Deployed MQL5 = windowed breakout + ADX/DI regime +
node-state absorption gate + MTF-M15/RSI quality gates; reversion OFF. **We port the MQL5 (ships), not
the Pine.** Full implementation spec written: `research/hypotheses/KK-MasterVP-SPEC.md`.

**Architecture decision (confirmed with user):** backtester does **NOT** call MT5 at all — it replays
the imported tick Parquet headlessly. **C++ = strategy core + tick engine** (fast, MQL-convertible);
**Python = harness only** (Optuna, parity diff, charts). KK-MasterVP EA has `InpExportParity` +
`InpExportTradeJournal` → use them as the **parity reference** to validate our engine reproduces PF
1.21/1.10 before optimizing.

**XAUUSD data imported** (162.8M ticks 2024–26, `data/processed/ticks_xauusd_*.parquet`). XAUUSD M3 is
the primary market for KK-MasterVP.

**MT5 Strategy-Tester output location (user's parity reference):**
`kenkem/Tester/Agent-127.0.0.1-3000/` — `logs/` for run logs (YYYYMMDD.log), and
`MQL5/Files/<strategy name>/` for the CSV trade/parity dumps. KK-MasterVP outputs:
`MQL5/Files/KK-MasterVP/parity_<sym>_<period>.csv` (per-bar) + `trades_<sym>_<period>.csv` (per-trade).
A reference already exists: BTCUSD-Exnes-0406 M3, day 2026-04-09 (480 bars, 75 raw signals, 4 trades).
Parity-CSV notes: only **master** VP cols (mpoc/mvah/mval) are valid — local poc/vah/val are 0/garbage
(reversion off → localCur never computed; last row has an uninitialized blowup). `sigValid` = RAW
DetectSignal (pre-gate) → validate signals independent of the safety/quality gates.

**Authoritative params = `KK-MasterVP/KK-MasterVP-baseline.set`** (NOT the code defaults — they differ
materially: NodeGate OFF, BreakBuf 0.50, RrBrk 1.8, SlAtrBrk 1.48, RiskAccPct 1.6, DailyDD 5.0).
Parity reference must be generated by running `KK-MasterVP.ex5` in MT5 tester (repo has the .ex5, the
.set, `Data/HighImpactNews_USD.csv`, and the `Parity/` exporters defining the CSV formats
`parity_<sym>_<tf>.csv` per-bar + `trades_<sym>_<tf>.csv` per-trade — our engine must emit byte-
compatible versions for diffing).

**C++ engine progress** (`dquants/cpp_core/`, dependency-free Makefile + clang C++20, `make test`):
DONE = build system, test harness, types (Tick/Bar/VPResult), `volume_profile.hpp` (exact
BuildVAFromHist + ComputeVP_Bar port), `indicators.hpp` (EMA/Wilder/ATR/RSI/ADX matching
pipeline/indicators.py). NEXT = NodeEngine, Regime, Strategy::DetectSignal (4 signals), PositionManager
(TP1/BE/runner-trail), RiskManager, Filters (session/news/ATR%/spread), TickEngine + ExecutionSimulator,
Parquet→binary data plumbing, baseline.set config loader, parity CSV emit + diff, then Optuna.
See [[discovery-findings]], [[pipeline-phase3-conventions]], [[btcusd-data-quirks]], [[project-kenkem-quant-os]].
